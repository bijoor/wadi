# Wadi - 3D Model Viewer

This folder contains the interactive 3D model viewer for the Wadi project.

## 🏠 View the Model

**[Open Interactive 3D Viewer](https://bijoor.github.io/wadi/)**

View the live 3D model at: https://bijoor.github.io/wadi/

## 📁 Files

- **index.html** - Interactive web viewer with controls
- **wadi.glb** - 3D model file (auto-generated by Python script)
- **README.md** - This file

## 🎮 Viewer Controls

- **Mouse Drag** - Rotate the model
- **Scroll Wheel** - Zoom in/out
- **View Buttons** - Quick camera presets (Top, Front, Side)
- **Auto-Rotate** - Toggle automatic rotation

## 🚀 Local Testing

Due to browser CORS restrictions, you need to run a local web server:

**Option 1: Python (recommended)**
```bash
# Navigate to the docs folder
cd docs

# Start a local server
python3 -m http.server 8000

# Open in browser: http://localhost:8000
```

**Option 2: VS Code**
- Install "Live Server" extension
- Right-click `index.html` → "Open with Live Server"

**Option 3: Node.js**
```bash
npx http-server docs -p 8000
```

Then open `http://localhost:8000` in your browser.

## 🔧 Technical Details

- **Format**: glTF 2.0 (GLB - binary)
- **Viewer**: Google Model Viewer
- **Browser Support**: Chrome, Firefox, Safari, Edge (modern versions)
- **No plugins required**: Works directly in the browser

## 📝 Updating the Model

The `wadi.glb` file is automatically generated by running the Blender Python script:

```python
# In Blender
export_to_web()
```

The HTML and CSS files are static and can be edited directly.

## 📝 About

This 3D model was generated using Blender and Python automation scripts.
The house design represents a traditional Konkan-style architecture with modern features.

---

Generated by Wadi Builder
